name: dot_matrix_text
description: "Creating retro-style LED dot matrix displays with customizable size, spacing, colors, and effects."
version: 0.2.0
homepage: https://github.com/topmoveright/dot_matrix_text

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter


screenshots:
  - description: 'Example screenshot'
    path: screenshots/screenshot1.png

topics:
  - text
  - ui-components
  - dot-matrix
  - led-text

flutter:
